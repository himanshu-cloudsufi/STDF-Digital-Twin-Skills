# Web chatbot dependencies for demand forecasting demo
# Install with: pip install -r requirements-web.txt

# Web framework
Flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.5

# WebSocket support
python-socketio==5.10.0
# Note: Using threading mode, eventlet is optional
# eventlet==0.33.3

# Claude Agent SDK
claude-agent-sdk>=0.1.0

# Environment configuration
python-dotenv==1.0.0

# Core forecasting dependencies (if not already installed)
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
